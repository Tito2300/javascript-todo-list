const form=document.querySelector("#task-form"),input=document.querySelector("#task-input"),list=document.querySelector("#task-list"),counter=document.querySelector("#task-counter"),clearBtn=document.querySelector("#clear-completed"),empty=document.querySelector("#empty-state"),KEY="todo-list-tasks";let tasks=load();function load(){try{return JSON.parse(localStorage.getItem(KEY))||[]}catch{return[]}}function save(){localStorage.setItem(KEY,JSON.stringify(tasks))}function render(){list.innerHTML="";tasks.forEach(t=>{const li=document.createElement("li");li.className="task-item"+(t.completed?" completed":"");const cb=document.createElement("input");cb.type="checkbox";cb.checked=t.completed;cb.addEventListener("change",()=>{t.completed=!t.completed;save();render()});const span=document.createElement("span");span.className="task-text";span.textContent=t.text;const del=document.createElement("button");del.className="delete-button";del.textContent="Delete";del.addEventListener("click",()=>{tasks=tasks.filter(x=>x.id!==t.id);save();render()});li.append(cb,span,del);list.appendChild(li)});const n=tasks.filter(t=>!t.completed).length;counter.textContent=`${n} ${n===1?"task":"tasks"} remaining`;empty.classList.toggle("hidden",tasks.length>0);clearBtn.disabled=!tasks.some(t=>t.completed)}form.addEventListener("submit",e=>{e.preventDefault();const text=input.value.trim();if(!text)return;tasks.push({id:Date.now().toString(),text,completed:false});input.value="";save();render()});clearBtn.addEventListener("click",()=>{tasks=tasks.filter(t=>!t.completed);save();render()});render();